"use client";

/**
 * Step 1 — authorisation & personal details (phone, full name, ID number, date
 * of birth, city, profile photo).
 *
 * Placeholder. `task-09` replaces this body with the real step; the signature
 * is already final — every step reads and writes through `useOnboardingDraft()`
 * rather than props, so the shell never has to change again once it lands.
 */
export function Step1AuthPersonal() {
  return (
    <div className="text-sm text-muted-foreground">Step 1 — coming soon.</div>
  );
}
