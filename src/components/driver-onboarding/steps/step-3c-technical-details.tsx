"use client";

/**
 * Step 3c — technical details (make/model, year, plate, colour, payload, cargo
 * hold dimensions). The shell routes to it on
 * `ONBOARDING_SCREENS.vehicleTechnical`.
 *
 * Placeholder. `task-12` replaces this body with the real step; the signature
 * is already final — every step reads and writes through `useOnboardingDraft()`
 * rather than props, so the shell never has to change again once it lands.
 */
export function Step3cTechnicalDetails() {
  return (
    <div className="text-sm text-muted-foreground">
      Step 3 details — coming soon.
    </div>
  );
}
