"use client";

/**
 * Step 2 — licence verification (front/back photos, number, expiry, categories).
 *
 * Placeholder. `task-10` replaces this body with the real step; the signature
 * is already final — every step reads and writes through `useOnboardingDraft()`
 * rather than props, so the shell never has to change again once it lands.
 */
export function Step2Licence() {
  return (
    <div className="text-sm text-muted-foreground">Step 2 — coming soon.</div>
  );
}
