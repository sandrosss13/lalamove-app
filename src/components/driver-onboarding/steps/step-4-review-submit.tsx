"use client";

/**
 * Step 4 — review & submit: the four summary cards and the submit CTA.
 *
 * Placeholder. `task-13` replaces this body with the real step; the signature
 * is already final — every step reads and writes through `useOnboardingDraft()`
 * rather than props, so the shell never has to change again once it lands.
 */
export function Step4ReviewSubmit() {
  return (
    <div className="text-sm text-muted-foreground">Step 4 — coming soon.</div>
  );
}
